#include <iostream>
using namespace std;

void function(int* array)
{
  cout<< array[0];
}

int main()
{
  int num =5;
  int num2 = 6;
  int* x ;
  int* y ;
  
  cout<< &x << endl << &y << endl;
  
  if(x==y){
    cout<< "Equal"; // Comparing memory address of x and y which are apparently different, but is coutting equal.why?
  }
  else
    cout<< "NOt equal";
    
  return 0;
}
